describe('swap.service', () => {
  it('Should initialize correctly', () => {});

  describe('batchSwapV1', () => {
    it('Should call exchange-proxy when swapping with exact inputs', () => {});

    it('Should call exchange-proxy when swapping with exact outputs', () => {});

    it('Should set the value to a hex string when native asset is passed in', () => {});
  });
});
